export const ATTACK_HIT =
  ":attackerId dealt :damage (:attackRoll :attackType + :attackModifier) damage to :targetId";

export function messageGenerator(
  template: string,
  params: Record<string, string>
) {}
